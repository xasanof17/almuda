import { Waveform } from "@uiball/loaders";
export default function Loading() {
  return (
    <div className="max-w-screen flex h-full min-h-screen w-full flex-col items-center justify-center">
      <Waveform />
    </div>
  );
}
